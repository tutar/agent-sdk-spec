# TODO

- 已完成：
  - 深入分析 `hooks` 源码，收敛了 event surface、matching model、execution runtime、result projection
  - 深入分析 `context` 源码，收敛了 system prompt composition、structured context、attachment assembly、memory recall、dynamic listing/delta
  - 已基于 `hooks` 深挖结果补强 `harness` 扩展面规范
  - 已基于 `context` 深挖结果重写 `harness/context-assembly/context-provider.md`
  - 已分析并补充 `plugin lifecycle / marketplace / extension packaging`
  - 已分析并补充 `SDK event transport / bridge projection`
  - 已分析并补充 `bridge / remote-control transport abstraction`
  - 已分析并补充 `bridge` 与 `session / transport / local runtime adapter` 的边界
- 当前待办：
  - 无
